package com.example.bankmisr.Test.domain;

import com.example.bankmisr.Test.domain.PlotDataConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlotModelConfigRepository extends JpaRepository<PlotDataConfig,Integer> {
}
